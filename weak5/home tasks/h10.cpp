#include<iostream>
using namespace std;
main(){
    char ch;
    cout<<"Enter the key 'y' to exit : ";
    cin>>ch;

    while(ch!='y') {
    cout<<"Enter the key 'y' to exit : ";
    cin>>ch;
    }
     

}